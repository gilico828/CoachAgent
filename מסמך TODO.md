,# מסמך TODO – Coach Agent

> רשימת משימות לפי Phase, מבוססת על ה-Roadmap ב-[מסמך דרישות.md](מסמך%20דרישות.md) וה-[מסמך ארכיטקטורה.md](מסמך%20ארכיטקטורה.md). טיוטה — Phase 1 מפורק לפרטים, שאר ה-Phases ברמת כותרת בלבד ויפורקו בתורם.

## Phase 1 — סוכן בסיסי בצ'אט (טלגרם) + זיכרון פר-משתמש

**היקף:** לולאת שיחה בסיסית מקצה לקצה. **בלי** tools (לא Nutrition, לא Food Log, לא Document). **בלי** DB — קבצים בלבד. משתמש יחיד.

**החלטות נעולות לשלב הזה:**

- ערוץ: טלגרם בלבד 
- הרצה: polling
- אחסון מסמך הוראות למשתמש: קובץ (JSON/Markdown) לפי `user_id`, לא DB.
- סטאק: Python + `python-telegram-bot` + Anthropic SDK + LangGraph (שכבת התזמור/agent orchestration נבנית כגרף — nodes/edges/state — במקום agent loop ידני).

**משימות:**

- [x] שלד repo — תיקיית פרויקט, `.env` (Telegram token, Anthropic API key), `requirements.txt`.
- [x] Telegram skeleton — בוט שמקבל הודעה ומחזיר echo (בלי LLM), לוודא שהחיבור עובד.
- [x] Claude API wrapper — פונקציה `call_agent(messages, system_prompt) -> reply`, נבדקת בנפרד מטלגרם.
- [x] LangGraph graph בסיסי — הקמת graph פשוט (node אחד לפי שלב: assemble prompt → קריאה ל-LLM → תשובה) שעוטף את `call_agent`, כבסיס להרחבה עתידית (tools, ענפים מותנים) ב-Phase 2 ואילך.
- [x] חיבור ל-LangSmith — הפעלת tracing על ה-LangGraph graph (משתני סביבה `LANGCHAIN_TRACING_V2`/`LANGCHAIN_API_KEY`), כדי לראות בפועל את זרימת ה-nodes, הפרומפטים והתשובות של כל אינטראקציה.
- [x] קובץ `general_instructions.md` — מסמך הוראות כללי (אישיות, כללי עבודה, גבולות, סגנון אימון).
- [x] קובץ הוראות פר-משתמש — `users/{user_id}.md` (מטרות, מגבלות, היסטוריה בסיסית). (Phase 1: משתמש יחיד, קובץ קבוע `users/gili.md`; מעבר ל-ID דינמי כשתהיה תמיכה ברב-משתמשים.)
- [x] הרכבת prompt — מיזוג general + user לכדי system prompt אחד.
- [x] זיכרון שיחה קצר-טווח — `dict[user_id] -> list[messages]` בזיכרון (לא persistent, נעלם בריסטארט — מקובל לשלב זה).
- [x] חיבור מלא — הודעה נכנסת מטלגרם → prompt assembly → Claude → תשובה → טלגרם.
- [x] בדיקה ידנית — שינוי `users/<id>.md` ווידוא שהתגובה משתנה בהתאם.
- [x] Dockerfile בסיסי — הרצת הבוט (polling) בתוך container במקום ישירות על המחשב; היכרות מוקדמת עם Docker גם אם עדיין אין orchestration אמיתי (זה מגיע ב-Phase 7 עם Kubernetes). כולל `.dockerignore` שמוציא במפורש את `coach_agent/users/*.md` מה-image (נתונים אישיים — מגיעים ב-runtime דרך volume mount, לא build-time).

**יציאה מה-Phase (Definition of Done):** אפשר לשוחח עם הסוכן דרך טלגרם, הוא עונה לפי ההוראות הכלליות + הוראות פר-משתמש, זוכר את השיחה הנוכחית, שכבת התזמור בנויה כ-LangGraph graph עם tracing ב-LangSmith, והכל רץ בתוך Docker container.

## Phase 2 — חיבור ל-API לבדיקת ערכים תזונתיים

- [x] בחירת ספק API — **USDA FoodData Central** (הושוותה מול Open Food Facts: OFF חזק בברקודים/מוצרים ישראליים אבל חלש במזון גנרי לא-ממותג כמו פרי בודד; USDA הפוך — נבחר USDA כי מזון גנרי היה החשוב יותר. חיפוש לפי ברקוד ב-USDA ממומש כ-full-text search + התאמת `gtinUpc`, כי אין endpoint ייעודי).
- [x] Nutrition Lookup Tool + חיבורו ל-tool-use של ה-LLM (`coach_agent/nutrition.py`, ענף מותנה חדש ב-`graph.py`). כולל גם חיפוש לפי ברקוד (טקסט בלבד — קלט תמונה/סריקה נדחה לעתיד).
- [x] בדיקה: המשתמש שואל "כמה קלוריות בבננה" והסוכן עונה דרך ה-API. נבדק גם עם ברקוד אמיתי ועם זיכרון רב-פנייתי (multi-turn) כדי לוודא שהזיכרון לא נשבר עם הודעות tool-use.

## Phase 3 — זיכרון טבלאי / יומן תזונה

- [x] Clock Tool — tool שמחזיר לסוכן את התאריך והשעה הנוכחיים (`get_current_datetime`). תנאי מקדים ליומן: "מה אכלתי היום" חסר משמעות כל עוד הסוכן לא יודע מה "היום", והמודל נוקב בתאריך מזיכרון האימון שלו בביטחון מלא אם לא אומרים לו במפורש לא להסתמך עליו. אזור הזמן נעול קשיח ל-`Asia/Jerusalem` — ה-instance ב-EC2 רץ ב-UTC, ו-`datetime.now()` בלי timezone היה מחזיר שעה שגויה בשקט, בלי שום שגיאה. (ב-Phase 5 אזור הזמן יהפוך לנתון פר-משתמש.) נוספה תלות `tzdata` ל-`requirements.txt`: `zoneinfo` קורא את מסד אזורי הזמן מהמערכת, ואין ערובה שהוא קיים ב-image.
- [x] איחוד הכלים ל-`coach_agent/tools.py` — כל כלי הוא פונקציה + הסכמה שלו + שורה ב-`_HANDLERS`, באותו קובץ; `nutrition.py` נבלע פנימה. `graph.py` הפך לגנרי (`run_tool(block["name"], ...)`) ולא יודע עוד אילו כלים קיימים, כך שהוספת כלי היא שינוי בקובץ אחד. נעשה תוך כדי ה-Clock Tool, כי הכלי השני הוא שחשף שה-dispatch היה מחווט לכלי יחיד.
- [ ] בחירת מסד נתונים (SQLite לשלב זה, מעבר ל-Postgres בעתיד אם צריך).
- [ ] סכמה ליומן (טבלה אחת, `meal_type` כעמודה — ראו [[dividing-food-log-table]]).
- [ ] Food Log Tool — כתיבה וקריאה.
- [ ] בדיקה: "אכלתי 2 ביצים" → נרשם; "מה אכלתי היום" → משוחזר מהיומן.

## Phase 4 — פריסה לענן (Cloud Deployment)

**היקף:** הוצאת הבוט (שכבר בנוי כ-Docker container בסוף Phase 1) מהמחשב האישי להרצה רציפה (24/7) בענן. שינוי תשתיתי בלבד — **בלי** שינוי בלוגיקת הסוכן, בלי tools חדשים.

**החלטות נעולות לשלב הזה:**

- ספק ענן: **AWS — EC2 instance**. שים לב: ה-Free Tier של AWS מוגבל בזמן (בניגוד ל-Always Free של Oracle), ואחרי שהוא נגמר המכונה מתחילה לעלות כסף (~5-10$ לחודש). נבחר בכל זאת במודע, מתוך רצון ללמוד את ספק הענן הנפוץ בתעשייה.
- הרצה: polling נשאר כמו שהוא (לא עוברים ל-webhook) — אין צורך ב-endpoint ציבורי/HTTPS.
- שיטת פריסה: SSH ל-instance + הרצת ה-container שכבר קיים מ-Phase 1 (`docker run`/`docker compose`), לא CI/CD אוטומטי בשלב הזה.
- ניהול secrets: קובץ `.env` על המכונה + `env_file` ב-compose. **לא** Secrets Manager / SSM Parameter Store בשלב הזה (נשקל שוב ב-Phase 7, כשתהיה תשתית אמיתית).
- ארכיטקטורת מעבד: אם ייבחר instance מסוג Graviton/ARM (`t4g`) — ה-image צריך להיבנות ל-`linux/arm64` (`docker buildx`). ב-instance x86 (`t3`) ה-image הקיים מ-Phase 1 עובד כמו שהוא.

**משימות:**

- [x] הקמת חשבון AWS + **הגדרת Budget Alarm לפני כל דבר אחר** — ה-Free Tier נגמר בשקט ו-AWS ממשיך לחייב בלי להתריע. (נעשה דרך התבנית המוכנה *Zero spend budget*; בנוסף הופעל MFA על ה-root user.)
- [x] יצירת Key Pair + הפעלת EC2 instance — region `eu-central-1` (פרנקפורט), Ubuntu Server 24.04 LTS, ארכיטקטורת `x86_64` (ולכן ה-image של Phase 1 רץ כמו שהוא — לא נדרש `buildx`).
- [x] Security Group — SSH (פורט 22) נכנס בלבד, מוגבל ל-IP הביתי (`Source type: My IP`). HTTP/HTTPS לא נפתחו.
  > כשה-IP הביתי מתחלף, החיבור נתקע בלי שגיאה ברורה — מעדכנים את הכלל ב-Security Group, לא מחפשים תקלה בשרת.
- [x] התקנת Docker על ה-instance — מהמאגר הרשמי של Docker (לא `apt install docker.io`), כולל `docker-compose-plugin`, והוספת המשתמש `ubuntu` לקבוצת `docker`.
- [x] העברת `.env` ל-instance ב-`scp` (Telegram, Anthropic, USDA ו-LangSmith) — לא דרך git.
- [x] העברת `coach_agent/users/gili.md` ל-`~/CoachAgent/data/users/` ב-`scp`. הקובץ מוחרג גם מ-git וגם מה-image (מידע אישי), ולכן מגיע כ-volume ב-runtime — בלעדיו ה-container קורס ב-`FileNotFoundError`.
- [x] העלאת ה-image — **הוכרע: build ישירות על ה-instance** אחרי `git clone` של ה-repo (ציבורי). ECR נחסך לגמרי, והעדכון בעתיד הוא `git pull` + `docker compose up -d --build`.
- [x] פריסת ה-container — דרך `docker-compose.yml` שנוסף ל-repo (`env_file`, volume ל-`data/users`, `restart`), במקום פקודת `docker run` ארוכה. פריסה = `docker compose up -d --build`.
- [x] מדיניות restart — `restart: unless-stopped` ב-compose. **נבדק בפועל:** אותחל השרת, וה-container חזר לבד תוך שנייה בלי התערבות. שתי השכבות נדרשות — `docker` מופעל ב-boot (`systemctl is-enabled docker`) *וגם* מדיניות ה-restart.
- [x] גישה בסיסית ללוגים — `docker compose logs -f` מתוך `~/CoachAgent` דרך SSH (`Ctrl+C` עוצר את הצפייה, לא את הבוט).
- [ ] בדיקת קבלה — הבוט מגיב בטלגרם לאורך זמן כשהמחשב האישי כבוי לגמרי. (הבוט אומת כעונה מהשרת; נותר לאמת לאורך זמן עם המחשב כבוי.)

**יציאה מה-Phase (Definition of Done):** הבוט רץ ברציפות על EC2 instance ב-AWS ללא תלות במחשב האישי, שורד restart של ה-instance, ויש Budget Alarm פעיל שמתריע לפני חיוב לא צפוי.

## Phase 5 — תמיכה ברב-משתתפים (Multi-user)

**היקף:** המעבר ממשתמש יחיד קבוע (`users/gili.md`) לריבוי משתמשים בו-זמנית על אותו בוט, כל אחד עם הוראות, זיכרון שיחה ויומן תזונה מבודדים משלו. בונה על ה-user_id שכבר קיים כמנגנון (Phase 1) וה-DB שהוקם ב-Phase 3.

**משימות:**

- [ ] מעבר מקובץ הוראות קבוע (`users/gili.md`) ל-user_id דינמי לפי Telegram chat_id.
- [x] תהליך onboarding — **שיחת אינטק**: משתמש חדש שב-allowlist מקבל ראיון של שלושה בלוקים שבסופו נכתבים שני מסמכים — פרופיל מתאמן (Markdown, סקשן-סקשן) והעדפות ליווי (JSON, enums). אותו גרף בדיוק, עם `system_prompt` ו-`tools` אחרים לפי `status` בקובץ המשתמש (`intake` / `active` / `blocked`). האינטק רץ על thread נפרד, כי שני המסמכים *הם* הסיכום שלו.
- [x] בידוד זיכרון שיחה קצר-טווח בין משתמשים שונים הפעילים בו-זמנית (`dict[user_id] -> messages`, לוודא שאין דליפה בין שיחות).
- [x] בידוד יומן התזונה (Phase 3) פר-משתמש במסד הנתונים.
- [x] בדיקת קבלה — שני משתמשים שונים משוחחים עם הבוט במקביל, כל אחד מקבל תשובות לפי ההיסטוריה וההוראות האישיות שלו בלבד.

**יציאה מה-Phase (Definition of Done):** מספר משתמשים יכולים לשוחח עם אותו בוט בו-זמנית, כל אחד עם הוראות, זיכרון שיחה ויומן תזונה נפרדים ומבודדים לחלוטין.

## Phase 6 — הכנת מסמכים משותפת (מטרות/דיאגרמות)

- [x] הגדרת פורמט אחסון למסמכים (קבצים בסגנון Markdown, כמו מסמכי הפרויקט עצמו).
- [x] Document Tool — יצירה/עריכה/שליפה.
- [x] תהליך שיחה מודרך ליצירת מסמך מטרות.

## Phase 7 — מעבר לפלטפורמה רחבה יותר (ווב/אפליקציה)

- [ ] בחירת טכנולוגיית ממשק.
- [ ] שכבת ממשק חדשה שמדברת מול אותו Agent Orchestrator (ללא שינוי בליבה — זו הנקודה של שכבת הממשק המנותקת).
- [ ] מעבר מ-in-memory/קבצים ל-DB אמיתי אם עוד לא בוצע.
- [ ] Kubernetes — פריסה/orchestration בסקאלה (multi-user, high availability), כתחליף ל-instance הבודד מ-Phase 4.

## Phase 8 — תפעול ועלות (Cost & Latency Management)

- [x] מעקב טוקנים/עלות — **הוכרע: דרך LangSmith, בלי לוג משלנו.** הסיבה שהעלות לא הופיעה עד עכשיו לא הייתה הגדרה חסרה ב-UI אלא שהנתון לא נשלח: LangGraph מתעד את ה-nodes שלו, והקריאה ל-Claude שבתוכם הייתה שקופה — כל ה-runs הגיעו כ-`chain` עם אפס טוקנים. העטיפה `wrap_anthropic` (כבר מותקן עם `langsmith`, בלי תלות חדשה) הופכת כל קריאה ל-run מסוג `llm` עם ה-usage שהתשובה מחזירה ממילא, ו-LangSmith מתמחר לבד — נבדק שהמחיר שהוא מחשב תואם בדיוק ל-$2/$10 למיליון של `claude-sonnet-5`, כך שאין צורך להזין מחירים ידנית. בנוסף נוסף `thread_id` ל-metadata של הגרף, כי בלעדיו יש עלות פר-הודעה אבל לא פר-שיחה. **נמדד:** 4,391 טוקנים קבועים בכל תור (system 3,739 + סכמות כלים 652) מול הודעת משתמש טיפוסית של ~26 טוקנים, כלומר ‎99% מהקלט הוא טקסט זהה שנשלח מחדש. הודעה אחת שמפעילה כלי נמדדה ב-$0.0215, ו-84% מזה קלט.
- [ ] בחירת מודל לפי משימה — למשל Haiku לשיחה פשוטה מול Sonnet/Opus למשימות מורכבות יותר (ניתוח תזונתי, הכנת מסמך).
- [ ] מדידת latency — זמן תגובה מקצה לקצה (הודעה נכנסת → תשובה), איתור צוואר הבקבוק (LLM call מול tool calls מול DB).
- [ ] Dashboard/ניטור בסיסי — ריכוז מדדי עלות/latency/שימוש, למשל דרך LangSmith או לוג מובנה.

## Phase 9 — זיכרון ארוך-טווח (Long-term Memory)

**היקף:** מה הסוכן זוכר בין שיחות, ואיך זה מגיע לקונטקסט בלי לשלם על כל ההיסטוריה בכל תור. **זהו שלב תכנון** — מה שכתוב כאן הוא *הצעות* ולא החלטות נעולות. המטרה היא שהדיון לא ילך לאיבוד, לא לנעול מימוש.

**המתח המרכזי:** לא רוצים שכל ההיסטוריה תיכנס לקונטקסט בכל פנייה (עלות), אבל כן רוצים שהסוכן יידע מה היה אתמול (תועלת). היום אין אף אחד מהשניים — הקונטקסט גדל בלי גבול *וגם* נמחק לגמרי בריסטארט.

**מה שכבר ידוע מהמדידה ב-Phase 8:**

- 4,391 טוקנים קבועים בכל תור, מהם 3,739 ה-system prompt.
- כלל האצבע שנגזר מזה: מה שנכנס ל-system prompt **נקנה מחדש בכל הודעה**; מה שנשלף לפי דרישה נקנה רק כשצריך אותו, במחיר round-trip נוסף. זה השיקול שמכריע את חלוקת השכבות למטה.

### פיצול השאלה — לא כל "אירוע" הוא בעיית זיכרון


| שאלה                       | איך היא נענית           | איפה זה נפתר             |
| -------------------------- | ----------------------- | ------------------------ |
| "מה אכלתי ביום שלישי שעבר" | שאילתה לפי תאריך        | **טבלת היומן — Phase 3** |
| "מה אמרתי לך על הכתף שלי"  | חיפוש בהיסטוריית השיחות | Phase 9                  |


השורה הראשונה היא הרוב המוחלט של "אירועים" בבוט אימון ותזונה, והיא **לא** בעיית זיכרון אלא שאילתה על טבלה. מנגנון זיכרון שמנסה לענות עליה בונה RAG כדי לעשות את מה ש-`WHERE` עושה מדויק וחינם.

**מסקנה:** Phase 9 מטפל רק בשארית הלא-מובנית. היא קטנה בהרבה ממה שנדמה בהתחלה.

### הצעה: מודל שלוש שכבות


| שכבה | מה יושב בה                                 | איפה            | עלות בטוקנים                     |
| ---- | ------------------------------------------ | --------------- | -------------------------------- |
| חם   | השיחה הנוכחית                              | קונטקסט         | נקנה בכל תור                     |
| חמים | פרופיל מזוקק — "מתאמן 3× בשבוע, ברך רגישה" | `users/{id}.md` | נקנה בכל תור, **אבל חסום בגודל** |
| קר   | תמלילי שיחות גולמיים                       | דיסק/DB         | 0 — לא נכנס לקונטקסט             |


**נקודת מוצא שכבר קיימת:** `users/{id}.md` *הוא כבר* השכבה החמימה — עמיד, פר-משתמש, וכבר בקונטקסט בכל פנייה. ההבדל היחיד מזיכרון "אמיתי" הוא שרק בן אדם כותב אליו. חלק ניכר מהשאלה הוא לא "איפה לאחסן" אלא **"האם הסוכן רשאי לכתוב לשם, ומה"**.

### הצעה: שלוש התנועות בין השכבות

```
חם (שיחה)  ──[סוף שיחה]──►  חמים (פרופיל)    ← קונסולידציה, מותר לה לאבד
    │
    └──────[סוף שיחה]──────►  קר (תמלילים)     ← ארכוב, בלי אובדן
                                   │
    ◄──────[tool לפי דרישה]────────┘           ← שליפה
```

**הטריגר לשתי הכתיבות הוא אותו אירוע: סוף השיחה.** לכן ה-idle timeout הוא לא פרט טכני אלא הדופק של כל המנגנון — בלי רגע שמוגדר כ"נגמר", אין מתי לזקק ואין מתי לארכב.

### הצעה: שליפה — התאריך הוא האינדקס, לא embedding

"שבוע שעבר" הוא סינון מדויק וזול. אם כל שיחה נשמרת עם חותמת זמן ו-user_id, השליפה היא tool רגיל:

```
recall(since="2026-09-09", until="2026-09-16", about="כתף")
```

מימוש מוצע בתוך ה-tool: סינון לפי תאריך (מדויק, חינם) → נותרות ~10 שיחות → קריאה אחת למודל זול (Haiku) שקוראת אותן ומחזירה את הרלוונטי. **אפס vector store, אפס embeddings.**

הודות ל-tool registry שנבנה ב-Phase 3, הוספת `recall` היא שינוי ב-`coach_agent/tools.py` בלבד.

**מתי RAG כן מרוויח את מקומו:** כששאלה מגיעה **בלי עוגן זמן** — "האם אי פעם הזכרתי בעיה בברך?" על פני חודשים. זה המקרה האמיתי, והוא נדיר. הסימן שהגענו אליו: הסינון לפי תאריך מפסיק לצמצם מספיק.

### העיקרון שמחזיק את הארכיטקטורה

**השכבה החמימה רשאית לאבד מידע בדיוק משום שהקרה לא מאבדת.**

הקונסולידציה יכולה לדרוס את הפרופיל בכל פעם ולתת למודל להחליט מה שורד — בלי חרדה, כי התמליל המלא שמור. זה מה שהופך את "לשמור תמלילים לדיסק" מצעד משעמם לצעד שמאפשר את כל השאר. **אם מדלגים עליו, כל החלטת זיקוק הופכת לבלתי הפיכה.**

### סיכונים לתכנן נגדם מראש

- **עובדה מהוזה בפרופיל לא נעלמת.** הקונסולידציה היא קריאת LLM ויכולה להמציא "עובדה", שנכנסת מאז לכל system prompt עתידי ומרעילה כל שיחה. מתגלה שבועיים אחר כך כשהבוט מתעקש על משהו שלא נאמר. הגנה: כל עובדה נושאת תאריך ("נאמר ב-14.9") — מאפשר גם התיישנות וגם אימות מול התמליל.
- **הסוכן עלול לדרוס הוראות אנושיות.** ההוראות שהמשתמש כתב (גבולות, סגנון אימון) והעובדות שהסוכן למד הם שני דברים עם בעלים שונים. סעיף נפרד בקובץ, או קובץ נפרד לגמרי — **לא כתיבה חופשית ל-**`users/{id}.md`.
- **markdown ולא vector store — יתרון תפעולי שקל לוותר עליו בטעות.** אפשר לפתוח קובץ ולקרוא מה הסוכן חושב שהוא יודע עליך. ב-embeddings אין את זה.
- **קונסולידציה תוך כדי שיחה מבטלת את ה-prompt cache.** פרופיל יציב הוא חלק מהפרפיקס ונתפס בקאש; פרופיל שמשתנה באמצע שיחה מאפס אותו לכל התור. טיעון נוסף לזקק **בסוף** שיחה ולא תוך כדי.

### סדר מימוש מוצע (חסר-חרטה)

1. **לשמור תמלילים.** זול, חסר-אובדן, אפס תשתית. גם אם לעולם לא נשלוף מהם — ברגע שהם שמורים, כל שאר האפשרויות נשארות פתוחות. **לא לדלג.**
2. **חילוץ בסוף שיחה.** קריאה אחת ל-LLM: "מה מהשיחה הזאת שווה לזכור לטווח ארוך?", והפלט נכתב לסעיף של הסוכן בפרופיל. זול (פעם בשיחה, לא בתור), והפלט הוא עובדות ולא קטעי דיאלוג.
3. **RAG — רק אם יתברר שצעד 2 מאבד דברים.** ואז התמלילים מצעד 1 כבר שם כדי לבנות עליהם.

כל צעד עומד בפני עצמו ואף אחד לא נועל את הבא אחריו.

### שאלות פתוחות להכרעה

- מה תקציב הטוקנים לשכבה החמימה, ומה קורה כשהיא מתמלאת?
- מי מחליט מה לזכור — הסוכן תוך כדי שיחה (tool בסגנון `remember`), או מעבר חילוץ בסוף שיחה? (ההצעה למעלה נוטה לשנייה, בין היתר בגלל ה-prompt cache.)
- כמה שעות idle מגדירות סוף שיחה? (הדיון עם דוד נטה ל-4-6 שעות, לפי האופי האפיזודי של הדומיין — צ'ק-אין בבוקר, דיווח בערב.)
- תמלילים בקבצים או ב-DB? (ה-SQLite של Phase 3 כבר יהיה שם.)
- האם להחליף את `MemorySaver` ב-`SqliteSaver` כדי שהשיחה הנוכחית תשרוד פריסה? תלות חדשה (`langgraph-checkpoint-sqlite`) + volume ב-compose; לא שורה אחת.

### משימות

- [ ] להכריע את מודל השכבות ואת תקציב הטוקנים לשכבה שנכנסת לקונטקסט.
- [ ] להגדיר idle timeout ואת פעולת הקונסולידציה שהוא מפעיל.
- [ ] persistence לתמלילים — תנאי מקדים לכל השאר, וזול בפני עצמו.
- [ ] `recall` tool עם סינון לפי תאריך.
- [ ] הפרדה מבנית בין הוראות אנושיות לעובדות שהסוכן למד.
- [ ] בדיקת קבלה — שיחה, המתנה מעבר ל-timeout, ואז שאלה שדורשת ידע מהשיחה הקודמת.

**תלויות:** בונה על ה-DB מ-Phase 3 ועל ה-tool registry שנבנה שם. קשור ל-Phase 5 (בידוד פר-משתמש) ול-Phase 8 (prompt caching).

## Phase 10 — קלט רב-מודאלי: תמונה וקול

**היקף:** המשתמש יכול לצלם את הצלחת או להקליט הודעה קולית במקום להקליד. שני הפיצ'רים יושבים ב-Phase אחד כי הם שוברים את **אותה הנחה** בדיוק: `main.py` רשום היום על `filters.TEXT & ~filters.COMMAND` ומעביר `update.message.text` כמחרוזת ל-`run_graph`. שניהם דורשים handler נוסף, הורדת קובץ מטלגרם, ושינוי חתימת הכניסה לגרף. אפשר לשחרר בשני חצאים (קול לבד פשוט יותר — הוא מתרגם לטקסט וחוזר לזרימה הקיימת).

**הבעיה הנוכחית, לפני כל פיצ'ר:** משתמש ששולח תמונה או הודעה קולית מקבל **שתיקה מוחלטת** — לא שגיאה, לא "לא הבנתי". ה-`MessageHandler` פשוט לא נרשם על סוגי ההודעה האלה. מבחינת המשתמש הבוט נתקע. זה מתוקן בעצם המשימה הראשונה למטה, גם לפני שיש יכולת אמיתית.

**החלטות פתוחות (להכריע לפני מימוש):**

- **מה עובר לגרף במקום** `str`**?** היום `run_graph(user_key, text, system_prompt)`. תמונה לא מצטמצמת לטקסט, ולכן צריך טיפוס קלט עשיר יותר (רשימת content blocks בסגנון ה-API, או dataclass פנימי שמתורגם ב-`graph.py`). ההכרעה הזו קובעת אם ערוץ עתידי (Phase 7) יצטרך לדעת על פורמט Anthropic — ראו [מסמך זרימת הודעה.md](מסמך%20זרימת%20הודעה.md).
- **ספק תמלול לקול** — API ענן (ספק חדש, מפתח חדש, ~$0.006 לדקה) מול מודל מקומי על ה-instance. **הנטייה: API ענן.** ה-instance מ-Phase 4 הוא free-tier צנוע, ותמלול מקומי בעברית עליו ידרוש מודל גדול יחסית שלא ירוץ שם בזמן סביר.

**משימות — תשתית משותפת:**

- [x] רישום handlers לתמונה וקול, ותשובת ברירת מחדל מנומסת לכל סוג הודעה שעדיין לא נתמך (סטיקר, וידאו, מיקום) במקום שתיקה. **בוצע לקול בלבד** (תמונה תגיע ב-PR שלה); ה-catch-all הוא `MessageHandler(filters.ALL, ...)` שרשום **אחרון**, כי dispatch עוצר ב-handler הראשון שמתאים.
- [x] הורדת קובץ מטלגרם (`await context.bot.get_file(...)`) — כולל תקרת גודל והתנהגות מוגדרת כשחורגים ממנה. התקרה היא **2 דקות לפי** `voice.duration`, ולא לפי בייטים: ה-duration מגיע בתוך ה-update בחינם, כך שהקלטה שנדחית לא עולה אף קריאת רשת. כשל הורדה נתפס כ-`TelegramError` ומקבל נתיב יציאה משלו.
- [ ] שינוי חתימת הכניסה לגרף כך שתישא קלט לא-טקסטואלי, בלי לחשוף את פורמט ה-API של Anthropic לשכבת הערוץ. **נדחה במכוון ל-PR של התמונה:** קול מצטמצם לטקסט, ולכן התמלול יושב *לפני* הגרף ו-`run_graph(user_key, text, system_prompt)` לא השתנה בכלל. ההכרעה הזו נכפית רק כשיש תמונה ביד.

**משימות — תמונה (Vision):**

- [ ] העברת תמונה ל-Claude כ-image block, כולל טיפול ב-`update.message.caption` (תמונה עם טקסט נלווה היא המקרה השכיח — "זה מה שאכלתי").
- [ ] `update.message.photo` הוא רשימה בסדר עולה של גדלים. **לא לקחת אוטומטית** `[-1]`**:** עלות הטוקנים של תמונה היא בערך `רוחב × גובה / 750`, כלומר תמונה אחת שקולה לאלפי טוקנים מול ~26 טוקנים של הודעת טקסט טיפוסית (נמדד ב-Phase 8). לבחור את הגודל הקטן ביותר שעדיין מאפשר זיהוי מזון, ולמדוד את ההפרש.
- [ ] **תמונה בהיסטוריית השיחה נקנית מחדש בכל תור.** ה-checkpointer שומר את ה-messages כמו שהם, ולכן תמונה שנשלחה פעם אחת ממשיכה להישלח לכל אורך השיחה. להחליט: להחליף את ה-image block בסיכום טקסטואלי אחרי התור, או לא לשמר אותו בכלל.
- [ ] שרשור זיהוי→כלי — המודל מזהה פריטים ומעריך כמויות, ואז קורא ל-`lookup_food` על כל פריט.
- [ ] **מדיניות אי-ודאות.** סעיף 5 בהוראות הכלליות אומר "לא לנחש ערכים תזונתיים כשיש אפשרות לבדוק". בתמונה המודל *חייב* להעריך כמות (גרמים) — זו הערכה ויזואלית, לא מדידה. לנסח בהוראות הכלליות מה מותר להעריך (נפח/משקל), מה חייב לעבור דרך ה-API (ערכים תזונתיים), ואיך להציג את ההערכה למשתמש כטווח ולא כמספר בודד.
- [ ] ברקוד מתמונה — `search_by_barcode` כבר קיים מ-Phase 2, אבל דורש היום הקלדה של 13 ספרות ביד. המודל קורא את הספרות מהתמונה וקורא לכלי הקיים; אין כלי חדש.
- [ ] בדיקה — צילום ארוחה מורכבת (כמה פריטים בצלחת) וצילום ברקוד, ואימות שהתשובה מבוססת על ה-API ולא על ניחוש.

**משימות — קול (Voice):**

- [x] בחירת ספק תמלול ובדיקתו **בעברית** ספציפית, על מונחי מזון ואימון. **נבחר Groq** ‏(`whisper-large-v3-turbo`, ‎$0.04/שעה, free tier של 8 שעות אודיו ביום — בפועל אפס). נמדד מול הקלטה אמיתית: turbo זול פי 2.8, מהיר ב-‎40%, ו*מדויק יותר* מ-`large-v3` אחרי priming — large-v3 התעקש על "פיטות" גם כשהמילה הנכונה הייתה ברמז. ה-endpoint תואם-OpenAI, כך שהחלפת ספק היא URL ומפתח.
- [x] טלגרם שולח `voice` כ-OGG/Opus — **Groq מקבל** `ogg` **ישירות.** אין המרה, אין `ffmpeg` ב-image, ואין תלות חדשה בכלל: הקריאה היא `httpx.post` אחד, ו-`httpx` כבר היה ב-`requirements.txt`.
- [x] **להחזיר למשתמש את מה שתומלל** — נשלח כהודעה **נפרדת ולפני** התשובה ("🎤 שמעתי: ..."), כך שהיא גם מסמנת סימן חיים בזמן שהגרף רץ.
- [x] טיפול בסוגי אודיו נוספים (`audio`, `video_note`) — **דחייה מפורשת** דרך ה-catch-all. הם לא עוברים תמלול; אם יתברר שמשתמשים שולחים אותם בפועל, `filters.VOICE` יורחב.
- [ ] בדיקה — הודעה קולית בעברית שמדווחת ארוחה, מקצה לקצה. **בוצע חלקית:** התמלול נבדק על הקלטה אמיתית בעברית ("אכלתי שתי פיתות עם פרגית" — מדויק מילה במילה, 0.8 שניות), ו-7 טסטים מכסים את נתיבי הכשל. מה שנותר לא-בדוק הוא החוליה `get_file` → `download_as_bytearray` → `transcribe`, שדורשת בוט חי בלי poller מתחרה.

**יציאה מה-Phase (Definition of Done):** אפשר לדווח ארוחה בתמונה או בהקלטה ולקבל תשובה מבוססת-API, אף סוג הודעה לא נענה בשתיקה, ועלות התמונה הנוספת נמדדה ב-LangSmith והובנה.

**תלויות:** בלי יומן התזונה (Phase 3) יש זיהוי אבל אין לאן לרשום — הפיצ'ר עובד, אבל חצי ממנו מתבזבז.

## Phase 11 — יוזמה מצד הסוכן (Proactive Check-ins)

**היקף:** הסוכן פונה למשתמש ביוזמתו — "לא שמעתי ממך מאתמול", "בוקר טוב, מה התוכנית להיום?", סיכום שבועי. זה ההבדל בין בוט שעונה לבין ליווי.

**למה זה ה-Phase הארכיטקטוני המעניין בפרויקט:** כל ריצה במערכת מתחילה היום מ-update נכנס מטלגרם. כאן, לראשונה, הגרף רץ **בלי שאף אחד כתב כלום** — ומכאן נגזרות כל השאלות למטה. זה גם מייצר בחינם חלק מהתשתית של Phase 9: מי שיודע לזהות "לא שמעתי ממך 18 שעות" יודע גם מתי שיחה נגמרה.

**החלטות פתוחות (להכריע לפני מימוש):**

- **מה מריץ את הגרף כשאין הודעה?** ה-`JobQueue` של `python-telegram-bot` (APScheduler מתחת) הוא הנתיב חסר-התלויות-החדשות, אבל הוא מגיע ב-extra ‏(`python-telegram-bot[job-queue]`) ולא בהתקנה הבסיסית — לוודא מול `requirements.txt` לפני שמניחים שהוא שם.
- **מה נכנס לגרף במקום הודעת משתמש?** הודעת trigger מסונתזת ("המשתמש שקט 18 שעות, זו פנייה יזומה"), או node כניסה נפרד. הראשון פשוט יותר אבל מזהם את היסטוריית השיחה בטקסט שהמשתמש לא כתב — ומשם הוא ייקנה מחדש בכל תור.

**משימות:**

- [ ] **הכיוון ההפוך של מיפוי הזהות.** הנתיב הנכנס בונה `user_key = telegram_<id>`; הנתיב היוצא צריך `chat_id` מתוך `user_key`. פירוק התחילית חייב להישאר ב-`main.py` בלבד, כמו הרכבתה — אחרת שכבת התזמור מתחילה לדעת מה זה "טלגרם". ראו [מסמך זרימת הודעה.md](מסמך%20זרימת%20הודעה.md).
- [ ] מדיניות פנייה פר-משתמש בקובץ הפרופיל: שעות שקטות, תדירות מקסימלית, וסוגי פנייה מאושרים. **הסיכון היחיד שיהרוג את הפיצ'ר הוא נודניקיות** — משתמש שחסם את הבוט לא חוזר.
- [ ] **אזור זמן פר-משתמש — כאן זה הופך מחובה.** הוא נעול היום קשיח ל-`Asia/Jerusalem` (Phase 3). כל עוד הסוכן רק *עונה*, שעה שגויה היא אי-נוחות; ברגע שהוא *יוזם*, היא "בוקר טוב" בארבע לפנות בוקר.
- [ ] פקודת ביטול מפורשת ומכובדת ("אל תפנה אלי מיוזמתך") שמכבה את זה לגמרי, בלי להתווכח עם המשתמש.
- [ ] **פרסיסטנטיות ל"מתי פניתי לאחרונה".** מדיניות ה-`restart: unless-stopped` מ-Phase 4 אומרת שה-container עולה מחדש לבד; אם הנתון הזה יושב בזיכרון, ריסטארט מאפס אותו — ולולאת ריסטארט הופכת לספאם. זה לא תרחיז קצה, זו ההתנהגות הרגילה של הפריסה הקיימת.
- [ ] טיפול בכישלון שליחה — משתמש שחסם את הבוט מחזיר `Forbidden`. לתפוס, לרשום ללוג, ולהפסיק לפנות אליו; לא לנסות שוב בלולאה.
- [ ] סיכום שבועי כפנייה יזומה ראשונה — מה נאכל, מגמת משקל, עקביות, ודבר אחד למקד בו. **זה גם מה שנותן סיבה לדווח מלכתחילה:** בלי פלט מצטבר, יומן תזונה מרגיש כמו עבודה בשביל אף אחד. מקום טבעי למודל זול (Phase 8) — ריצה אחת בשבוע על נתונים מובנים.
- [ ] בדיקת קבלה — משתמש שותק יום שלם ומקבל פנייה אחת בשעה סבירה; משתמש פעיל לא מקבל פנייה מיותרת; ריסטארט של ה-container באמצע לא מייצר פנייה כפולה.

**יציאה מה-Phase (Definition of Done):** הסוכן פונה מיוזמתו לפי מדיניות מוגדרת פר-משתמש, מכבד שעות שקטות ואזור זמן, שורד ריסטארט בלי ספאם, ואפשר לכבות את זה בהודעה אחת.

**תלויות:** בונה על Phase 3 — בלי יומן אין על מה להעיר, והפנייה מתנוונת ל"מה שלומך?" גנרי. קשור ל-Phase 9: הטריגר "המשתמש שקט X שעות" הוא אותו idle timeout שמגדיר שם סוף שיחה.