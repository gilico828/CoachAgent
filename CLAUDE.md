# Coach Agent

## מטרת הפרויקט
פיתוח מערכת Agent המשמשת כמאמן כושר ותזונה אישי (AI Coach).
לצד הפיתוח עצמו, מטרה נוספת ומרכזית של הפרויקט היא למידת תפקיד ה-AI Engineer דרך עבודה מעשית ארכיטקטורת agents, עבודה עם כלים (tools), ניהול context/memory, ותהליכי פיתוח סביב מערכות מבוססות LLM.

קובץ זה יתעדכן באופן שוטף תוך כדי העבודה על הפרויקט.

## חוקים
1. בכל פעם שנוצר מסמך חדש בפרויקט (קובץ תיעוד, החלטה ארכיטקטונית, סיכום וכו') — לעדכן את הקובץ הזה (Claude.md) בהתאם, כדי שישקף את המצב העדכני של הפרויקט.

## מסמכים בפרויקט 
המסמכים האלה הם טיוטת לכן אפשר לערער על החלטות שלהם ועדכן אם יש צורך 
- [מסמך דרישות.md](מסמך%20דרישות.md) — טיוטת דרישות ראשונית של המערכת (ממשקי תקשורת, שכבות הוראות, יכולות מרכזיות, שאלות פתוחות, Roadmap).
- [מסמך ארכיטקטורה.md](מסמך%20ארכיטקטורה.md) — טיוטת ארכיטקטורה: שכבות המערכת (ממשק/ערוצים, תזמור-סוכן, כלים, זיכרון-נתונים, אינטגרציות), דיאגרמת שכבות ודיאגרמת רצף לדוגמה.
- [מסמך TODO.md](מסמך%20TODO.md) — רשימת משימות לפי Phase (מבוססת על ה-Roadmap); Phase 1 מפורק למשימות קונקרטיות, שאר ה-Phases ברמת כותרת.
- [מסמך נושאי לימוד.md](מסמך%20נושאי%20לימוד.md) — מיפוי נושאי AI Engineering שנלמדים דרך הפרויקט (כולל LangGraph), לפי Phase ולפי נושאים חוצי-פרויקט; מסומן ✅ ככל שמתקדמים.
- [coach_agent/prompts/intake_instructions.md](coach_agent/prompts/intake_instructions.md) — שכבת ההוראות של הסוכן במצב אינטק (מחליפה את `general_instructions.md`, לא רצה לצידה): מבנה שלושת הבלוקים, מוסכמת `לבירור`/`לא ידוע`, כללי גזירת מסמך ההעדפות, וקווים אדומים.
- [coach_agent/prompts/template_trainee.md](coach_agent/prompts/template_trainee.md) — התבנית הריקה של פרופיל המתאמן. הכותרות שלה הן החוזה של הכלי `save_trainee_section`.
- [מסמך מסד נתונים.md](מסמך%20מסד%20נתונים.md) — ההחלטות מאחורי שכבת הנתונים הטבלאית (Phase 3): למה DB אחד משותף ולא אחד לכל מתאמן, מי מחזיק את אוצר המילים של השדות (ולמה לא המודל), כלל הקידום של `notes` לעמודה, ולמה זמן נשמר מקומי ולא UTC. הסכמה עצמה חיה ב-`coach_agent/log_store.py`.
- [DEPLOY.md](DEPLOY.md) — runbook לפריסה: התחברות ל-EC2, מה יושב על השרת, עדכון גרסה, לוגים, והתקלות הצפויות. נכתב ב-Phase 4.


## Skills בפרויקט
- **דוד** (`.claude/skills/david/`) — יועץ system-design עם פרסונה קבועה, עונה בקצרה (עמדה → 1-3 בולטים → צעד הבא). Scope: פרויקט זה בלבד. שימוש: לפנות אליו בשם ("דוד, ...") או בכל שאלת ארכיטקטורה/tradeoff. רשימת שיקולים מורחבת (scalability, coupling, data ownership וכו') נמצאת ב-`references/checklist.md` ונטענת רק לפי הצורך.
- **גון** (`.claude/skills/gon/`) — מוביל תהליך מפריט ב-מסמך TODO.md ועד ל-PR מלא: בחירת משימה, branch, חלוקת כתיבת קוד בין המשתמש לגון (באישור), כללי כתיבת קוד לפרויקט, עדכון TODO/נושאי לימוד, ופתיחת PR. Scope: פרויקט זה בלבד. שימוש: לפנות אליו בשם ("גון, ...") או כל בקשה לקחת/להתחיל משימה מה-TODO.
- **נועה** (`.claude/skills/noa/`) — מהנדסת AI שחוקרת לפני שהיא מחליטה: יוצאת ל-GitHub ולרשת, מביאה ראיות מסומנות (נצפה / מומלץ / דעה), מתלבטת עם המשתמש, ומסכמת ב-artifact HTML של המערכת. Scope: פרויקט זה בלבד. שימוש: לפנות אליה בשם ("נועה, ...") או בכל שאלת "מה ה-best practice" / "איך עושים את זה בעולם" / השוואת frameworks ותבניות. Playbook המחקר (מקורות, שאילתות `gh`, מסנני איכות) נמצא ב-`references/research_playbook.md` ונטען לפי הצורך. לא לתשובה מהירה בלי מחקר (זה דוד) ולא למימוש עד PR (זה גון).
