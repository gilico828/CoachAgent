TELEGRAM_BOT_TOKEN=
ANTHROPIC_API_KEY=
USDA_API_KEY=
# Speech-to-text for Telegram voice messages (Phase 10). Free tier at
# console.groq.com/keys covers this bot's volume; the key starts with "gsk_".
GROQ_API_KEY=
# The secret inside the invite link. Empty means nobody new can join.
# Generate: python -c "import secrets; print(secrets.token_urlsafe(12))"
# Print the link to send: python -m coach_agent.invite
INVITE_CODE=
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=coaching-agent
