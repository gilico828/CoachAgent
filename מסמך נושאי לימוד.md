# מסמך נושאי לימוד – Coach Agent

> טיוטה. מיפוי של נושאי AI Engineering שנלמדים דרך העבודה על הפרויקט, לפי [מסמך TODO.md](מסמך%20TODO.md) ו-[מסמך ארכיטקטורה.md](מסמך%20ארכיטקטורה.md). מתעדכן ומסומן ✅ ככל שמתקדמים.

## 1. נושאים חוצי-פרויקט

נושאים שחוזרים ומתעמקים לאורך כל ה-Phases, לא קשורים ל-Phase בודד.

- [ ] **Agent orchestration / agent loop** — ליבת שכבת התזמור: הרכבת prompt → קריאה ל-LLM → החלטה על tool → תגובה.
- [ ] **Prompt engineering / הפרדת שכבות הוראות** — `general_instructions.md` מול `users/{id}.md`, ניסוח persona, כללי גבולות, סגנון אימון.
- [ ] **Tool use / function calling** — המנגנון של Claude API להחליט מתי לקרוא tool ומתי לענות ישירות.
- [ ] **LangGraph** — framework לבניית agent orchestration כגרף (nodes/edges/state), ניהול state מובנה וזרימת שליטה בין שלבי הסוכן; חלופה/תוספת למימוש agent loop ידני.
- [ ] **LangSmith** — observability/tracing ל-LangGraph: רואים כל ריצה של הגרף (nodes, פרומפטים, תשובות, זמני ריצה), בסיס מעשי ל"בדיקה/אבלואציה" למטה במקום debug ידני על stdout.
- [ ] **בדיקה/אבלואציה של התנהגות LLM** — evaluation-driven development סביב agents.
- [ ] **ניהול secrets / קונפיגורציה** — `.env`, הפרדת secrets מקוד.



## 2. נושאים לפי Phase



### Phase 1 — סוכן בסיסי + זיכרון פר-משתמש (נוכחי)

- [ ] **Channel adapter pattern** — Telegram Bot API, polling vs webhook, תרגום פורמט חיצוני↔פנימי אחיד.
- [ ] **Claude API wrapper** — קריאה בסיסית ל-LLM, ניהול messages array, הפרדת system prompt מ-conversation.
- [ ] **Short-term context management** — `dict[user_id] -> list[messages]`, הבדל מ"זיכרון" ארוך-טווח.
- [ ] **Prompt assembly** — מיזוג שתי שכבות הוראות למחרוזת system אחת.



### Phase 2 — Nutrition API

- [ ] **External API integration מתוך tool** — schema של tool (input/output), error handling.
- [ ] **בחירת ספק API** — trade-offs בין ספקי נתוני תזונה (עלות, כיסוי, דיוק).



### Phase 3 — יומן תזונה טבלאי

- [x] ✅ **Tool registry / dispatch לפי שם** — המעבר מכלי יחיד למספר כלים: הסכמה וה-handler יושבים יחד, שכבת התזמור מנתבת לפי `name` ולא יודעת אילו כלים קיימים.
- [x] ✅ **Grounding של המודל במצב שהוא לא יכול לדעת** — תאריך/שעה הם המקרה הנקי: המודל לא חש אי-ודאות ולכן לא יפנה לכלי מעצמו, וה-`description` חייב לשלול במפורש הסתמכות על ידע פנימי.
- [ ] **Database schema design** — טבלה עם `meal_type` כעמודה (ראו [[dividing-food-log-table]] ב-TODO).
- [ ] **מעבר מ-in-memory לפרסיסטנטי** — SQLite, tool שקורא/כותב ל-DB בלי לחשוף זאת לשכבת ה-LLM.
- [ ] **Retrieval פשוט מתוך structured data** — query על נתונים טבלאיים, בהבדל מ-RAG.



### Phase 4 — פריסה לענן (AWS)

- [x] ✅ **IaaS מול PaaS מול serverless** — למה VM בודד (EC2) ולא App Runner/Fargate כשמדובר בתהליך polling שרץ ברציפות.
- [x] ✅ **מודל ה-Free Tier של ספקי ענן** — ההבדל בין tier מוגבל-בזמן (AWS) ל-Always Free (Oracle), ולמה Budget Alarm הוא משימה ראשונה ולא אחרונה.
- [x] ✅ **EC2 basics** — region, AMI, instance type, Key Pair, Elastic IP.
- [x] ✅ **רשת ואבטחה בסיסית בענן** — Security Group כ-firewall, חשיפת SSH בלבד, למה polling חוסך את כל נושא ה-inbound.
- [x] ✅ **ניהול secrets בפרודקשן** — `.env` + `--env-file` כנקודת התחלה, ומה ההבדל מ-Secrets Manager / SSM Parameter Store (נדחה ל-Phase 7).
- [ ] **ארכיטקטורת מעבד ו-container images** — x86 מול ARM/Graviton, `docker buildx --platform`, למה image שנבנה על לפטופ עלול לא לרוץ על השרת. (לא נלמד בפועל — ה-instance שנבחר הוא x86 וה-image רץ כמו שהוא. יחזור אם/כשנעבור ל-Graviton.)
- [x] ✅ **אמינות בסיסית** — `--restart unless-stopped` / systemd, שרידות ל-reboot, גישה ללוגים מרחוק.



### Phase 5 — רב-משתמשים

- [ ] **בידוד state פר-משתמש** — למה `dict[user_id]` גלובלי מספיק לשלב הזה ומתי הוא נשבר.
- [x] **Onboarding של משתמש חדש** — שיחת אינטק שכותבת את ההוראות של הסוכן עליו.
- [x] **Mode כ-state ולא כגרף שני** — אותם nodes עם `system_prompt` ו-`tools` אחרים; הכלי שלא ניתן למודל הוא הגבלה חזקה יותר מהוראה שנאמרה לו.
- [x] **המסמך כ-state של השיחה** — מה שנשמר לדיסק הוא גם הפלט וגם מצב ההתקדמות, ולכן אינטק שנקטע ממשיך מאיפה שעצר בלי checkpointer מתמיד.
- [x] **טקסט של משתמש שנכנס ל-system prompt** — enums במקום טקסט חופשי, והשדה החופשי היחיד מגיע fenced ומתויג כמידע ולא כהוראה.



### Phase 6 — מסמכים משותפים

- [ ] **Structured output מה-LLM** — תשובת צ'אט חופשית לעומת ייצור/עריכת מסמך מובנה.
- [ ] **Guided multi-turn flows** — state machine קל לעומת שיחה חופשית.
- [ ] **Document Tool כ-CRUD interface** — יצירה/עריכה/שליפה כפונקציות tool נפרדות.



### Phase 7 — פלטפורמה רחבה יותר

- [ ] **בדיקת ה-decoupling בפועל** — הוכחה אמפירית שהחלפת ערוץ (Telegram → web) לא דורשת שינוי ב-Agent Orchestrator.
- [ ] **מעבר קבצים → DB אמיתי** (אם עוד לא קרה ב-Phase 3).



### Phase 9 — זיכרון ארוך-טווח

- [ ] **ארכיטקטורת זיכרון של agent** — ההבדל בין *קונטקסט* (מה שנשלח למודל בפנייה) לבין *זיכרון* (מה שהמערכת מחזיקה), וחלוקה לשכבות לפי עלות: מה נקנה בכל תור, מה נשלף לפי דרישה.
- [ ] **Memory consolidation** — זיקוק שיחה לעובדות בסוף שיחה, והסיכון המרכזי: עובדה מהוזה שנכנסת לפרומפט קבוע ומרעילה כל שיחה עתידית.
- [ ] **מתי RAG הוא לא הכלי הנכון** — שליפה לפי מטא-דאטה (תאריך, user_id) מול שליפה סמנטית; למה שאילתה על נתונים מובנים לא צריכה embeddings.
- [ ] **Context budgeting** — ניהול תקציב טוקנים בין שכבות, והקשר ההדוק ל-prompt caching (מה שמשתנה תוך כדי שיחה מאפס את הקאש).

### Phase 10 — קלט רב-מודאלי (תמונה וקול)

- [ ] **Multimodal input ל-LLM** — העברת תמונה כ-content block לצד טקסט, וההבדל בין מודל שמזהה (perception) למודל שיודע (knowledge): הזיהוי הוא הערכה ויזואלית, והערך התזונתי חייב להגיע מכלי חיצוני.
- [ ] **עלות טוקנים של תמונות** — למה תמונה אחת שקולה לאלפי טוקנים, ולמה רזולוציה היא החלטת עלות ולא רק החלטת איכות. בהמשך ישיר לפרופיל העלות שנמדד ב-Phase 8.
- [ ] **מדיה בהיסטוריית שיחה** — תמונה שנשמרת ב-checkpointer נקנית מחדש בכל תור; הבחירה בין לשמר, לסכם, או לזרוק.
- [ ] **הרחבת חוזה הקלט של הסוכן** — המעבר מ-`str` לקלט מובנה בלי שהערוץ (Telegram) יתחיל לדעת על פורמט ה-API של המודל. מבחן אמיתי ל-decoupling שנטען ב-[מסמך זרימת הודעה.md](מסמך%20זרימת%20הודעה.md), לפני Phase 7.
- [x] **Speech-to-text כשלב מקדים** ✅ — תמלול אינו חלק מהסוכן אלא נורמליזציה של קלט לפניו. בפועל: `transcription.py` מקבל bytes ומחזיר str, ו-`run_graph` לא השתנה בשורה אחת — זה המבחן שהנורמליזציה באמת יושבת *מחוץ* לסוכן. התמלול חוזר למשתמש כהודעה נפרדת, כי בלעדיה שגיאת תמלול מייצרת תשובה קוהרנטית לחלוטין על משהו שהוא לא אמר.
- [x] **ביצועי מודל בעברית** ✅ — נמדד ולא הונח, וההפתעה הייתה כפולה: המודל הגדול (`large-v3`) הפסיד לקטן ממנו (`turbo`) על עברית, ו**רמז אוצר המילים עוזר רק למילים שנמצאות בתוכו** — "פרגיט" הפך ל"פרגית" רק אחרי שהמילה נכנסה לרמז. מסקנה מעשית: prompt priming הוא פרמטר תחזוקה, לא הגדרה חד-פעמית.

### Phase 11 — יוזמה מצד הסוכן

- [ ] **Agent שרץ ללא user turn** — היפוך כיוון הזרימה: עד כאן כל ריצה התחילה מהודעה נכנסת. מה נכנס לגרף כשאין מה שהמשתמש אמר, ואיפה זה יושב בהיסטוריה.
- [ ] **Scheduling כחלק ממערכת agent** — trigger מבוסס זמן/מצב לצד trigger מבוסס הודעה, והשאלה מי מחזיק את המצב "מתי פניתי לאחרונה" כשהתהליך יכול לעלות מחדש בכל רגע.
- [ ] **מדיניות פנייה כנתון פר-משתמש** — שעות שקטות, תדירות, opt-out ואזור זמן; למה אצל סוכן יוזם אזור זמן מפסיק להיות פרט תפעולי והופך לתנאי לשימושיות.
- [ ] **הנתיב היוצא של מודל הזהות** — `user_key` → כתובת בערוץ, והשמירה על כך שידע הערוץ לא ידלוף לשכבת התזמור דרך הדלת האחורית.

## 3. תשתית / הרצה (Infra & Deployment)

נושאים שקשורים לאיפה ואיך המערכת רצה בפועל, ולא לליבת הסוכן עצמה.

- [x] ✅ **Docker** — containerization של האפליקציה (הסוכן + תלויות), סביבת הרצה עקבית בין מחשב פיתוח לשרת. מתחילים כבר ב-**Phase 1** (Dockerfile בסיסי לבוט ה-Telegram), כדי לא לדחות את כל נושא ה-infra לסוף. עונה גם על השאלה הפתוחה "היכן ירוץ הסוכן" ב[מסמך דרישות.md](מסמך%20דרישות.md).
- [ ] **Kubernetes** — orchestration והרצה בסקאלה (multi-user, high availability). נשאר ב-**Phase 7**, כי עד אז אין עדיין צורך אמיתי בסקאלה — container בודד (Docker) מספיק.

## 4. תפעול ועלות (Cost & Latency) — Phase 8

נושאים שקשורים להפעלה כלכלית/יעילה של הסוכן בפועל, לא רק ל"זה עובד".

- [x] ✅ **מעקב טוקנים/עלות** — מאיפה מגיע נתון ה-usage, ולמה observability של הגרף (LangGraph nodes) אינה observability של ה-LLM: בלי עטיפת ה-client אין run מסוג `llm`, ובלי טוקנים אין מה לתמחר.
- [x] ✅ **קריאת פרופיל עלות** — פירוק הקלט לחלק הקבוע (system + סכמות כלים) מול החלק המשתנה, וההבנה שביחס קלט/פלט קיצוני העלות נקבעת כמעט כולה במה שנשלח מחדש בכל תור.
- [ ] **Prompt caching** — caching של חלקי הפרומפט הקבועים (הוראות כלליות/פר-משתמש) בקריאות ל-Claude API, לחיסכון בעלות ו-latency.
- [ ] **בחירת מודל לפי משימה** — Haiku/Sonnet/Opus trade-offs בין מהירות/עלות לאיכות, לפי סוג המשימה.
- [ ] **מדידת latency** — זמן תגובה מקצה לקצה, איתור צוואר הבקבוק בין LLM call, tool calls ו-DB.
- [ ] **Dashboard/ניטור בסיסי** — ריכוז מדדים (עלות, latency, שימוש), בהמשך ישיר ל-LangSmith שכבר נלמד ב-Phase 1.

## 5. איך עובדים עם המסמך הזה

- כשמתחילים לעבוד על נושא בפועל (קוד/משימה), מסמנים אותו כ-in-progress בשיחה.
- כשהנושא "נלמד" בפועל (יש קוד עובד + הבנה של איך ולמה) — מסמנים ✅ כאן.
- אפשר להוסיף נושאים חדשים שיתגלו תוך כדי עבודה (למשל נושא לא צפוי שעולה ב-Phase מסוים).

