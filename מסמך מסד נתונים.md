# מסד הנתונים — למה הוא בנוי ככה

המסמך הזה מסביר את ההחלטות מאחורי שכבת הנתונים הטבלאית (Phase 3), לא את הסכמה עצמה —
הסכמה נמצאת ב-`coach_agent/log_store.py` והיא המקור האמיתי. כאן נמצא **למה** היא כזו,
כי זה החלק שלא נקרא מתוך הקוד, וזה גם החלק שיהיה צריך לערער עליו בעתיד.

---

## 1. למה DB אחד משותף, ולא אחד לכל מתאמן

האינסטינקט הראשון היה קובץ נפרד לכל משתמש — עקבי עם מה שכבר קיים, שבו לכל אדם יש
`users/<user_key>.md` משלו. הוא נדחה, אבל לא מהסיבה שנראית ראשונה.

**האינסטינקט הזה מערבב שני דברים נפרדים:**

| | מה זה | מה פותר אותו |
|---|---|---|
| **בידוד** | שהנתונים של אחד לא יגיעו לשני | קובץ נפרד **או** עמודה + משמעת גישה |
| **פרסונליזציה** | שכל אחד יעקוב אחרי מדדים אחרים | סכמה גמישה — **לא קשור לקובץ נפרד** |

DB נפרד פותר את הראשון בלבד. את השני הוא לא נוגע: אם לכולם אותה סכמה — לא הרווחנו דבר,
ואם לכל אחד סכמה אחרת — קיבלנו את בעיית ה-migration הגרועה ביותר שיש, כי כל שינוי חייב
לרוץ N פעמים וכישלון באמצע משאיר אוכלוסייה עם שתי סכמות בלי דרך קלה לדעת מי במה.

ההמלצה הרווחת בספרות היא shared schema כברירת מחדל, ומעבר ל-database-per-tenant רק
כשיש דרישת compliance, scale או customization שמחייבת — ראו
[Bytebase](https://www.bytebase.com/blog/multi-tenant-database-architecture-patterns-explained/)
ו-[dasroot](https://dasroot.net/posts/2026/01/multi-tenancy-database-patterns-schema-database-row-level/).
המונחים לחיפוש הם `multi-tenancy` ו-`shared schema vs database-per-tenant`.

### מה זה עולה, ואיך משלמים

החיסרון של DB משותף אמיתי ולא תיאורטי: הבידוד נשען על `WHERE user_key = ?` בכל שאילתה.
`WHERE` חסר אחד, והמאמן מספר לאדם אחד מה אדם אחר אכל — כשל שקט, בלי חריגה ובלי שורת לוג.

**המענה הוא ארכיטקטוני:** `log_store.py` הוא **המקום היחיד בפרויקט שכותב SQL**, וכל
פונקציה ציבורית בו מקבלת `user_key` כפרמטר ראשון. אין פונקציה שאפשר לקרוא לה בלי לומר
עבור מי. `tools.py` לא מכיל שאילתה אחת. זו אותה הפרדה ש-`profile_store.py` עושה למסמכים,
והיא גם מה ששומר על כלל השכבות ב-[מסמך ארכיטקטורה.md](מסמך%20ארכיטקטורה.md).

הטסט הראשון ב-`tests/test_log_store.py` בודק בדיוק את זה, והוא החשוב בקובץ.

---

## 2. מי מחזיק את אוצר המילים

**הכלל: המודל לא ממציא שמות של שדות.**

אם נותנים ל-LLM לכתוב מפתחות חופשי, על פני ארבע שיחות מתקבלים `feeling`, `feeling_after`,
`how_felt` ו-`mood_post_meal`. כל אחד תקין בפני עצמו, כל אחד נשמר בהצלחה, ואף אחד לא
נשבר. ואז השאלה "תראה לי ארוחות שהרגשתי רע אחריהן" פשוט לא ניתנת למענה: הנתונים נראים
עשירים והם לא-שאילתיים. זה כישלון שמתגלה חודשים אחרי שנוצר.

לכן `metric` ב-`log_measurement` הוא **enum בסכמת הכלי**, ו-`log_store.METRIC_UNITS` הוא
הרשימה הסגורה. מדד שלא ברשימה מוחזר למודל כשגיאה — תור אחד מבוזבז, לא שיחה מתה — בדיוק
כמו הטיפול בסקשן לא קיים ב-`save_trainee_section`.

זה גם מה שקורה ברמת העמודות: שדה שהמודל שלח ואינו ב-`_ITEM_FIELDS` **נזרק ולא נשמר**.

---

## 3. `notes`, וכלל הקידום

אם המודל לא ממציא שדות, מה קורה כשמישהו אומר משהו שאין לו עמודה?

**הוא הולך ל-`notes` כטקסט חופשי.** "אמר שהרגיש כבד אחרי" נכנס כפרוזה. שום מידע לא הולך
לאיבוד, ושום אוצר מילים לא נסחף.

**וכשאותו דבר חוזר ב-`notes` מספיק פעמים — זה הסימן לקדם אותו לעמודה אמיתית.**
אוצר המילים גדל על ראיות, לא על ניחוש מראש.

### למה זה עובד: הנחה שבדקנו ולא החזיקה

ההיגיון הרגיל אומר שצריך לקבוע את העמודות מראש, כי להוסיף עמודה אחר כך זה יקר. **ב-SQLite
זה פשוט לא נכון:**

- `ALTER TABLE ADD COLUMN` בלי constraints הוא **O(1)** — *"the execution time of such ALTER
  TABLE commands is independent of the amount of data in the table and such commands will run
  as quickly on a table with 10 million rows as on a table with 1 row"*
  ([lang_altertable](https://www.sqlite.org/lang_altertable.html)).
- עמודה שרובה NULL כמעט לא עולה כלום: serial type 0 תופס **0 bytes** בגוף הרשומה
  ([fileformat2](https://www.sqlite.org/fileformat2.html)).

כלומר אין צורך לנחש את קבוצת-העל של העמודות, ואין צורך בעמודת JSON כדי להישאר גמישים.
המנגנון בפועל הוא רשימת `_MIGRATIONS` ב-`log_store.py` עם `PRAGMA user_version`: הוספת
עמודה בעתיד היא **איבר נוסף ברשימה, ותו לא**.

(אם בכל זאת יידרשו בעתיד שדות לא-מספריים ומשונים: ל-SQLite יש JSON מובנה מגרסה 3.38.0
ועמודות generated שניתן לאנדקס. דלת פתוחה — לא נדרשת עכשיו.)

---

## 4. שתי הטבלאות, ולמה כל אחת בצורה שלה

### `food_log` — עמודות קבועות, שורה לכל פריט

ערכים תזונתיים הם קבוצה **סגורה**: קלוריות, חלבון, פחמימות, שומן. זה לא משתנה בין אנשים,
ולכן אלה עמודות אמיתיות.

**שורה לכל פריט ולא לכל ארוחה:** הערכים התזונתיים הם פר-פריט, `lookup_food` מחזיר פר-פריט,
ו"כמה חלבון אכלתי היום" הופך ל-`SUM` במקום ל-parse. הארוחה עצמה שורדת כ-`eaten_at` המשותף
לשורות שלה. ההחלטה הזו גם עקבית עם ההכרעה המוקדמת יותר של טבלה אחת עם `meal_type` כעמודה,
במקום טבלה לכל סוג ארוחה.

### `measurements` — טבלה ארוכה

`(user_key, measured_at, metric, value, unit)`. `metric` הוא ערך בשורה ולא עמודה בסכמה.
היום `weight` ו-`body_fat`; מחר `sleep_hours` או `resting_hr` — **שורה חדשה, לא migration**.

זו טכנית תבנית **EAV**, ולתבנית הזו יש שם רע. השם הרע מוצדק כשמפרקים *ישות* לשורות
ומרכיבים אותה חזרה בג'וינים. מדידות הן מקרה אחר: כל ערך הוא מספר עם יחידה וחותמת זמן,
הומוגני לגמרי, ואף פעם לא צריך להרכיב ממנו ישות. הבעיות הקלאסיות לא נושכות כאן.
רקע: [Leapcell](https://leapcell.io/blog/storing-dynamic-attributes-sparse-columns-eav-and-jsonb-explained).

### זמן מקומי ולא UTC

`eaten_at` נשמר כ-ISO-8601 **בשעון ישראל**, עם היסט. הסיבה: כל שאלה שנשאלת על הנתונים
האלה היא שאלה על **יום מקומי** — "מה אכלתי היום" — ומחרוזת מקומית עונה עליה בהשוואת טווח
שנכונה בשני חצאי השנה. UTC היה דוחף חישוב היסט משתנה לתוך כל שאילתה.

⚠️ **זו ההכרעה היחידה כאן שכואב לשנות אחרי שיש שורות בטבלה.** כשאזור הזמן יהפוך לפר-משתמש
(Phase 5), תתווסף עמודת `tz` — ולא תתבצע המרה של הקיים.

---

## 5. מה נשמר תמיד, מול מה נשאל ומוצג

ההפרדה הזו עוד לא ממומשת בקוד, אבל הסכמה בנויה כדי לאפשר אותה, וכדאי שתיוותר ברורה:

| | מה נשמר | מה נשאל ומוצג |
|---|---|---|
| מי מחליט | **אף אחד — תמיד הכל** | העדפת המשתמש |
| הפיכות | לשמור הפיך, לא-לשמור **בלתי**-הפיך | מתהפך בשנייה |

**כלל: העדפת משתמש לעולם לא שולטת במה שנשמר.** מי שאומר "לא מעניין אותי חלבון" עדיין
מקבל `protein` בטבלה — `lookup_food` החזיר אותו ממילא — הוא פשוט לא שומע עליו. אם בעוד
שלושה חודשים הוא ישנה את דעתו, הופכים דגל אחד **וההיסטוריה כבר שם**. אם היינו מצייתים
מילולית, היינו נותנים לו טבלה ריקה בלי דרך לשחזר.

**הצעד הבא המתוכנן** הוא `tracked_fields` ב-`users/<user_key>.coach.json` — מה נשאל ומוצג
לאדם הזה. הצנרת כבר קיימת במלואה: `update_coach_preferences` נמצא בשתי ערכות הכלים,
`write_coach_preferences` ממזג שדה-שדה, ו-`build_system_prompt` קורא את התוצאה בכל הודעה.
זו לולאה שבה כלי שהסוכן קורא משנה את ההוראות של הסוכן בתור הבא.

ועיקרון נלווה: **הסוכן מציע, המשתמש מחליט, הכלי מתעד.** סוכן שמשנה העדפה בשקט יוצר מצב
שבו הבוט הפסיק לשאול על משהו ואף אחד לא יודע למה. הקונפיג צריך להיות תיעוד של הסכמה.

---

## 6. מה מכוון לשינוי

הדברים שסביר שיזוזו, וכיצד:

- **עמודות ב-`food_log`** — יתווספו לפי כלל הקידום. איבר ב-`_MIGRATIONS`.
- **`METRIC_UNITS`** — יגדל. הוספת מדד לא נוגעת בסכמה בכלל.
- **`tracked_fields`** — ראו סעיף 5.
- **אזור זמן פר-משתמש** — Phase 5. עמודת `tz`, בלי המרת עבר.
- **מ-SQLite ל-Postgres** — לא צפוי. נשקל מחדש רק אם יופיע תהליך שני שכותב.
- **`MemorySaver` → `SqliteSaver`** — שאלה פתוחה ב-[מסמך TODO.md](מסמך%20TODO.md).
  אם ייעשה, **בקובץ נפרד מ-`coach.db`**: checkpoints ניתנים לזריקה ויומן תזונה לא, ולכן
  יחס הגיבוי שלהם שונה לגמרי.

  ⚠️ שימו לב לסתירה בין מקורות: התיעוד הרשמי של LangChain מתאר את `SqliteSaver` כאחסון
  *"for development"* ([persistence](https://docs.langchain.com/oss/python/langgraph/persistence)),
  בעוד מקורות אחרים אומרים שהוא תקין בתהליך יחיד ושהבעיה מתחילה רק ב-multi-replica
  ([ActiveWizards](https://activewizards.com/blog/langgraph-state-management-checkpointing-recovery-and-the-persistence-layer-decision/)).
  הבוט רץ כתהליך יחיד, ולכן הצד השני כנראה רלוונטי כאן — אבל זה לא הוכרע.
